---
import Layout from "~/layouts/PageLayout.astro";
import Hero from "~/components/widgets/Hero.astro";

export async function getStaticPaths() {
  const response = await fetch(
    "https://www.globalization-partners.com/wp-json/wp/v2/posts",
  );
  const posts = await response.json();

  return posts.map((post) => ({
    params: { slug: post.slug },
  }));
}

const { slug } = Astro.params;

const response = await fetch(
  `https://www.globalization-partners.com/wp-json/wp/v2/posts?slug=${slug}&_embed`,
);
const posts = await response.json();

if (!posts || posts.length === 0) {
  return Astro.redirect("/404");
}

const post = posts[0];

let postContent = post.content?.rendered || "";

if (!postContent && post.acf) {
  const possibleFields = ["content", "post_content", "main_content", "body"];
  for (const field of possibleFields) {
    if (post.acf[field]) {
      postContent = post.acf[field];
      break;
    }
  }
}

const metaDescription =
  post.yoast_head_json?.description || "Description not available.";

const yoastTitle = post.yoast_head_json?.title || "Title not available";
const yoastDescription =
  post.yoast_head_json?.description || "Description not available";

const videoUrl = "https://vital.wistia.com/embed/medias/gs9xea1har";

const metadata = {
  title: yoastTitle,
  description: metaDescription,
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <Hero
    actions={[
      {
        variant: "primary",
        text: "Back to post list",
        href: "../",
        target: "",
        icon: "",
      },
      { text: "Learn more", href: "#features" },
    ]}
    image={{
      style: { borderRadius: "8px", width: "50%", height: "auto" },
      src: "~/assets/images/gp.png",
      alt: "gp",
    }}
  >
    <Fragment slot="title">{yoastTitle}</Fragment>
    <Fragment slot="subtitle">{yoastDescription}</Fragment>
  </Hero>

  <div
  style="
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    margin: 20px auto;
    max-width: 1200px;
    padding: 20px;
    box-sizing: border-box;
  "
>
  <div
    style="
      flex: 1;
      padding-right: 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    "
  >
    <h2
      style="
        font-size: 1.8rem;
        margin-bottom: 16px;
        font-weight: bold;
        text-align: left;
      "
    >
      Lorem Ipsum
    </h2>
    <p
      style="
        font-size: 1.2rem;
        line-height: 1.6;
        color: #333;
        text-align: left;
      "
    >
      Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod
      tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim
      veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea
      commodo consequat. Duis aute irure dolor in reprehenderit in voluptate
      velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat
      cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id
      est laborum.
    </p>
  </div>

  <div
    style="
      flex: 1;
      display: flex;
      justify-content: center;
      padding-left: 40px;
      margin-top: 90px;
    "
  >
    <iframe
      style="width: 100%; max-width: 500px; height: 280px;"
      src="https://vital.wistia.com/embed/medias/gs9xea1har"
      title="Vídeo Wistia"
      frameborder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
    ></iframe>
  </div>
</div>

<style>
  @media (max-width: 768px) {
    div[style*="display: flex"] {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    div[style*="padding-right: 40px"] {
      padding-right: 0;
    }

    div[style*="padding-left: 40px"] {
      padding-left: 0;
      margin-top: 20px;
    }

    h2 {
      font-size: 1.5rem;
    }

    p {
      font-size: 1rem;
    }

    iframe {
      width: 100%;
      height: auto;
      max-width: 100%;
    }
  }
</style>

  <style>
    .post-content {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    .post-content img {
      max-width: 100%;
      height: auto;
    }
  </style>

  <script define:vars={{ post, postContent, yoastTitle, yoastDescription }}>
    console.log("Post data (client-side):", post);
    console.log("Post content (client-side):", postContent);
    console.log("Yoast Title (client-side):", yoastTitle);
    console.log("Yoast Description (client-side):", yoastDescription);
  </script>
</Layout>
